# Copy to /etc/dino/production.env on AEZA.
# Keep this file OUTSIDE the Git repository in production.

# Domain served by the first block of the Caddyfile.
APP_DOMAIN=dino.example.com

# Direct PostgreSQL URL reachable from the AEZA host itself.
# The password must be URL-encoded if it contains reserved URL characters.
# Keep the value quoted.
SUPABASE_DB_URL='postgresql://postgres:CHANGE_ME@127.0.0.1:5432/postgres'
